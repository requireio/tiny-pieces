{
  "name": "at-path",
  "description": "Robustly access a nested value from an object path using a dot-delimited string.",
  "version": "0.0.2",
  "scripts": {
    "test": "node test"
  },
  "devDependencies": {
    "tape": "^2.6.0"
  },
  "license": "private",
  "publishConfig": {
    "registry": "http://localhost:8889/"
  }
}
